recipe:
  Rx: recipes/schema
  args:
  description: schema recipe
install_files: >
  __default.custom.yaml
  terra_pinyin.custom.yaml
patch_files:
  default.custom.yaml:
    - patch/+: { __include: __default.custom.yaml:/patch }
  terra_pinyin.custom.yaml:
    - patch/+: { __include: terra_pinyin.custom.yaml:/patch }
